# -*- mode: snippet -*-
# name: consumer-stateless-widget
# key: cslw
# --
class ${1:name} extends ConsumerWidget {
  const $1({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container($2);
  }
}