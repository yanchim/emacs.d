# -*- mode: snippet -*-
# name: cls
# key: cls
# --
class ${1:`(file-name-base
             (or (buffer-file-name)
                 (buffer-name)))`} ${2:extends ${3:Parent}} ${4:implements ${5:Interface}} {
                 $0
}