# -*- mode: snippet -*-
# name: public class extends implements
# key: pclei
# --
public class ${1:`(file-name-base
                    (or (buffer-file-name)
                        (buffer-name)))`} extends ${2:Parent} implements ${3:Interface} {
                        $0
}