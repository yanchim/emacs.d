# -*- mode: snippet -*-
# name: pcls
# key: pcls
# --
public class ${1:`(file-name-base
                    (or (buffer-file-name)
                        (buffer-name)))`} ${2:extends ${3:Parent}} ${4:implements ${5:Interface}} {
                        $0
}