# -*- mode: snippet -*-
# name: class extends
# key: cle
# --
class ${1:`(file-name-base
             (or (buffer-file-name)
                 (buffer-name)))`} extends ${2:Parent} {
                 $0
}